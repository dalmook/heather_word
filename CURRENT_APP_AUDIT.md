# CURRENT_APP_AUDIT

작성: 2026-09-05. 기준: `41ccac96d04d32a114879dbbb43e2f726a94385f` (main). 앱 소스 변경 전 작성.

## 목적과 보존 경계
Heather Word 9.0은 빌드 없는 정적 영어 학습 앱이다. 이미 5탭 셸, 기존 기능 브리지, 시즌2 모험을 갖추고 있다. 이번 작업은 이 기준보다 기능을 줄이지 않는 UX/UI 재설계다. 결제 서비스나 새 백엔드는 추가하지 않는다. 운영 main과 Firebase 데이터는 테스트 대상으로 수정하지 않는다.

## 구조
`index.html`은 기존 화면 DOM, `app.js`는 학습·보상·상점·펫·아바타·관리 로직이다. `ui-v9.js`는 5탭·보호자 PIN·집중 화면을 연결하고 `ui-v9-core.js`는 표시용 어댑터다. `season2-core.js`는 숙련도·모험·부화·진화 계산, `season2.js`는 모험 화면이다. `style.css`, `season2.css`, `ui-v9.css` 3계층의 역사적 스타일이 공존한다. `firebase-config.js`는 로더와 시즌2 저장 병합 보호를 제공한다.

`words.json`은 현재 빈 배열이다. 실사용 단어는 저장 데이터나 기존 Firebase에서 들어오므로 제품 화면에 테스트 단어를 넣지 않는다. 테스트 픽스처는 별도 브라우저 컨텍스트에서만 사용한다.

## 화면과 사용자 흐름
첫 진입 → 저장/연결 상태 → 홈 → 모험 또는 학습/게임 → 카테고리와 방식 → 문제 → 정오답 → 다음 → 완료/보상 → 복습·재시작. 컬렉션은 60종 시즌2·1000종 기존 도감·펫·드레스룸을 모두 제공한다. MY에는 기록·쿠키샵·프로필·설정·보호자 관리가 있다. 보호자 관리에서 단어·카테고리·백업/복원을 다룬다.

## 확인한 문제
- 홈에서 서로 다른 일일 미션과 모험 진행률을 `Math.max`로 합쳐 보여 주어 의미가 불분명하다. 첫 실행에서 disabled CTA가 유일한 큰 행동이며 준비 경로가 약하다.
- 단어장 검색은 입력마다 main 전체를 재생성해 입력 포커스를 잃는다. 결과를 120개에서 자르고 다음 페이지가 없다.
- 기록이 없는 사용자에게도 복습을 “마쳤다”고 안내한다. 게임 카테고리를 미리 고르기 어렵다.
- 큰 장식·그라디언트·반복된 파트너 그림·여러 색 카드가 학습 행동보다 시각적으로 강하다. 작은 보조 글자와 한 줄 말줄임이 많다.
- 전체 render가 보이지 않는 1000종 도감·상점까지 재생성한다. UI 셸이 1.2초마다 저장소를 읽는다.
- 시즌2 리포트만 고정 비밀번호를 사용해 보호자 PIN과 정책이 다르다. 기기 PIN은 서버 인증이 아니라는 안내는 유지해야 한다.
- 집중 화면 hash 새로고침·브라우저 앞/뒤 탐색 처리가 불완전하다. 시즌2는 비네이티브 dialog의 키보드 포커스 격리가 약하다.
- 메타/아이콘은 있으나 독립 404와 manifest가 없다. 전체 live-region과 일부 이름/색상 대비 문제가 있다.

## 기준 검증 증거
`npm run check`: 기존 28개 테스트 통과, 60종 SVG 검사 통과. GitHub Actions run `33935708392`에서 하위 경로 `/heather_word/`의 기존 브라우저 시나리오 통과. JS exception/console error/HTTP 4xx·5xx 기록은 0. Lighthouse: Performance 70 / Accessibility 91 / Best Practices 96 / SEO 100. 이 수치는 LOCAL 모드 Chromium 실험값이며 운영 Firebase·실기기 보증이 아니다. 기존 브라우저 스위트는 4게임 전체 분기·실결제·실기기 TTS 등을 완전히 커버하지 않는다.

## 데이터와 기술부채
`heather_word_v3`, `heather_parent_gate_v1`, player 필드와 시즌2 schema8/호환 경로를 유지한다. Firestore 권한 파일을 배포하지 않는다. 클라이언트 기록 랭킹은 서버 검증 랭킹이 아니며 공유 단어장 권한·실사용 개인정보/운영정책·신뢰 가능한 결제/랭킹은 별도 출시 게이트다. Firebase 실제 권한 및 iOS/Android 가상키보드·음성·기기 성능은 수동 검증을 남긴다.

## 기능 전체 인벤토리
아래 40개 기능군의 상세 동작·관련 파일·데이터·재검증 방법은 `FEATURE_REGRESSION_CHECKLIST.md`에서 관리한다. 이 목록을 기준으로 삭제 대신 유지/재배치한다.
