# Heather Word Season 2 implementation audit

Work branch: `codex/heather-word-season2`

This document is generated as part of the compatibility-first implementation. Existing local storage key `heather_word_v3`, existing score/XP/cookie balances, the 1,000-entry legacy collection, avatar/shop/pet ownership, word library data, LOCAL mode, Firebase mode, and the Phaser DOM fallback are treated as protected behavior.

Implementation commits and verified test output will be recorded here before the branch is fast-forwarded to `main`.
