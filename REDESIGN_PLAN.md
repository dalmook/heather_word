# REDESIGN_PLAN

기준: main `41ccac96`, 작성: 2026-09-05. 앱 소스 수정 전 확정한 구현 계획.

## 제품 방향
Quiet learning, friendly progress: 차분한 밝은 표면, 보라 한 가지 주 행동 색, 넓은 글줄과 여백. 캐릭터는 기존 학습 보상 콘텐츠로 유지하되 탐색 화면을 지배하지 않게 한다. 기존 5탭에 익숙한 사용자를 위해 홈/학습/게임/컬렉션/MY의 주소와 기능은 유지하고 각 화면의 우선순위·배치를 다시 설계한다.

## 정보 구조 / 내비게이션
홈: 한 가지 다음 학습 행동 → 실제 모험 단계 → 오늘 미션의 개별 항목 → 내 학습 수치. 빈 단어장에서는 보호자 도구로 준비하는 명확한 CTA.
학습: 복습 필요 여부 → 카드 → 검색 가능한 전체 단어장 → 카테고리. 검색창은 업데이트 중 재생성하지 않고 60개 단위 더보기 제공.
게임: 학습할 카테고리 선택 → 뜻/블록/빈칸/쓰기 4종과 실제 점수 → 별도의 4단계 모험.
컬렉션: 현재 파트너 → 시즌2 60종 → 기존 1000종·펫·드레스룸. 어떤 도감도 합쳐 삭제하지 않는다.
MY: 실제 기록 → 꾸미기·상점 → 프로필/소리/학습설정 → 기기 보호자 도구·리포트·동기화. 보호자 PIN 확인 경로를 하나로 통일한다.
학습 집중 화면: 상단 복귀·진행, 중앙 문제, 하단 입력/피드백. 뒤로가기·재진입과 새로고침은 저장 가능한 상태만 복원하고 지원하지 않는 재개를 주장하지 않는다.

## 디자인 시스템
색: primary #5B43C4 / secondary #38655A / accent #A46C1C / background #F7F8FA / surface #FFFFFF / border #DDE1E8 / text #202537 / secondary-text #5A6475 / success #25654B / warning #8B5A0B / error #B83244.
타입: 시스템 한글 산세리프. display 36, h1 28, h2 21, h3 18, body 16, caption 13, button 15, number 28px; 반응형 display만 유동 조정. 숫자는 tabular-nums.
간격: 4/8/12/16/24/32/48/64px. Radius: 8/12/20/999px. Animation: 180ms, reduced-motion 우선. 그림자는 dialog 등 부유층에만 제한.
구성요소: button/icon button, action row, section, progress, badge, text field/select, dialog, live toast, empty/loading/offline 상태. 모바일 48px 이상 주요 터치 영역, focus-visible·semantic heading·색과 글자 동시 피드백.

## 컴포넌트 구조
토큰·공통 컴포넌트·앱 레이아웃/뷰·기존 기능 어댑터 CSS를 파일로 분리한다. 기존 ui-v9.css를 다시 정의하고 장식 패치를 덧붙이지 않는다. 기존 순수 계산 모듈과 데이터는 보존한다. 새 표시용 템플릿/이벤트만 분리하고 필요한 성능 개선은 숨은 무거운 화면을 그리지 않는 범위로 제한한다. 기존 SVG/Phaser 기하와 게임 동작에 필요한 style.css/season2.css는 호환 경계로 유지한다.

## 모바일 전략
360/375/390/412/430px에서 single column, 하단 5탭. 768px은 넓어진 본문, 980px부터 명확한 텍스트 사이드 내비게이션과 2열 홈. 1440px에서도 콘텐츠가 과도하게 늘어나지 않도록 max-width 적용. safe-area와 visualViewport 지원 유지. 길고 복잡한 단어/이름은 줄바꿈, 긴 목록은 점진 로드, dialog는 100dvh 제한과 내부 스크롤.

## 기능 보존 / 상태 / 성능
기능 체크리스트 F01–F40을 전후 비교한다. 점수·문제 선택·리비전 병합·미션·보상·진화는 바꾸지 않는다. 프로덕션 단어/기록을 생성하지 않는다. 첫 실행·빈 검색·disabled·정오답·완료·오프라인·로드 실패·잘못된 PIN 상태를 구현/확인한다. 새로운 서비스워커로 캐시나 Firebase 정책을 바꾸지 않으며 콜드 오프라인 설치를 보장하지 않는다.
1000종 숨은 도감은 해당 화면에서 렌더한다. 검색 결과만 갱신하고 미사용 메인 전체 렌더를 줄인다. 기존 CSS를 대폭 재작성하되 아바타/게임 호환 코드는 보존한다. Lighthouse 네 카테고리를 전후 측정한다.

## 검증 및 출시 게이트
기존 28개 테스트를 계속 실행하고 새 표시·검색·PIN·내비게이션·성능 보호 테스트를 추가한다. Chromium에서 실제 하위 경로로 신규/기존 사용자·정오답·완료·네 게임·컬렉션·관리·입력 포커스·모바일 레이아웃·화면 캡처를 실행한다. CI에는 한글 폰트를 설치해 시각 결과를 검토한다. 점수는 실제 실행 결과만 기록한다.
별도 브랜치 `redesign/commercial-20260905`와 PR에서 검토하며 main/운영 Firebase는 변경하지 않는다. 배포는 PR 검토 후 기존 main/root 방식으로 수행한다. 변경이 사용자 기록 필드를 삭제하지 않아 코드 롤백이 가능하다. 실제 유료 출시 판정은 이 UI 작업만으로 대신하지 않는다.
